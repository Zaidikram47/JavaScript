const userId = 1234;
let userName = "Zaid Ikram";
let userEmail = "zaidikram.g610s@gmail.com";
let cityName = "Rahim Yar Khan";

/*
    use const for constant (unchangable) values
    use let for variables (changable) values
    dont use var as it has no scope despite it works as a variable but still prefer let
*/

console.table([userId,userName,userEmail,cityName]);
